//전체, Web, App, Game... 필터 버튼 모음
import React from 'react';

const MainCategoryFilter = () => {
  return (
    <div>
      {/* 여기에 전체, Web, App, Game 등의 필터 UI */}
      카테고리 필터입니다.
    </div>
  );
};

export default MainCategoryFilter;
